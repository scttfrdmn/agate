# --- Secrets / local config (NEVER commit) ---
.env
.env.*
!.env.example
*.pem
*.key
aws-exports.json
.agate.json
!.agate.example.json

# --- Python / uv ---
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.mypy_cache/
*.egg-info/
dist/
build/

# --- CDK ---
infra/cdk.out/
cdk.out/
cdk.context.json
.cdk.staging/

# --- Node / web ---
node_modules/
web/dist/
*.tsbuildinfo
.vite/

# --- Go ---
cli/agate
cli/bin/
*.test
*.out

# --- Deploy artifacts ---
# Baked per-model rates written at deploy time by `python -m cost.pricelist`
# (#90). Generated, not source — regenerated before each deploy.
cost/model_rates.json

# --- Editor / OS ---
.DS_Store
.idea/
.vscode/
*.swp
